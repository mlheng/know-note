# requirements-ocr.txt - PaddleOCR 可选依赖（仅本地高性能 OCR）
# Streamlit Cloud 免费版不支持 PaddleOCR（包体积过大），请勿部署
# 云端部署请使用 requirements.txt 中的 pytesseract + packages.txt
# 本地安装: pip install -r requirements-ocr.txt

paddlepaddle==2.6.2
paddleocr==2.8.1
numpy<2.0
opencv-python<4.10
scikit-image
shapely
